# Housekeeping

- [ ] lint | src/tags.rs:102 | `gather_env_tags` signature exceeds rustfmt line width (pre-existing from T3 restructure; `cargo fmt --check` fails there and across most `tests/*` files) | fix | observed in fix-repo-tag-worktree
